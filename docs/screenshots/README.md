# Screenshots

Ảnh dùng cho phần hero của README. Thêm 2 file vào thư mục này:

| File | Nội dung |
|---|---|
| `editor.png` | Giao diện chỉnh sửa chính (timeline + preview). |
| `export.png` | Hộp thoại Export. |

**Mẹo:** chụp lại từ app đang chạy (`npm run dev`) để thanh tiêu đề hiển thị đúng **XinChao-Cut**, đừng dùng ảnh còn tên cũ. Nên crop gọn, rộng ~1280px, dung lượng < 500 KB mỗi ảnh.

---

# Screenshots (EN)

Hero images for the README. Drop two files here:

| File | Content |
|---|---|
| `editor.png` | Main editor view (timeline + preview). |
| `export.png` | Export dialog. |

**Tip:** recapture from the running app (`npm run dev`) so the window title reads **XinChao-Cut**. Crop tight, ~1280px wide, keep each under ~500 KB.
